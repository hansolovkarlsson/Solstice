program TestProgramheadingArbitrary(input, output, somethingelse);
{ Expected output: Hello - the parameter list is a no-op, so any
  identifier is accepted, not just input/output }
begin
    writeln('Hello');
end.
