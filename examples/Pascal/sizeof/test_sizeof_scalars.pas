program TestSizeofScalars;
begin
    writeln(sizeOf(integer));
    writeln(sizeOf(real));
    writeln(sizeOf(boolean));
    writeln(sizeOf(byte));
    writeln(sizeOf(shortint));
    writeln(sizeOf(word));
end.
